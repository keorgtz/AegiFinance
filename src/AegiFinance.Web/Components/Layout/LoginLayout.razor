@inherits LayoutComponentBase

<div class="login-layout">
    <main>
        @Body
    </main>
</div>

<style>
    .login-layout {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-color: var(--md-sys-color-surface);
        background-image: radial-gradient(circle at top right, var(--md-sys-color-primary-container) 0%, transparent 40%),
                          radial-gradient(circle at bottom left, var(--md-sys-color-tertiary-container) 0%, transparent 40%);
    }

    .login-layout main {
        width: 100%;
        max-width: 420px;
        padding: 2rem;
    }
</style>
